name: Build Vision Pro App

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-15  # Use macOS 15 runner for VisionOS compatibility

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Select Xcode 16.2
        run: |
          echo "Switching to Xcode 16.2..."
          sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer
          xcodebuild -version

      - name: Create Vision Pro App Project
        run: |
          echo "Creating Vision Pro App Project..."
          mkdir -p VisionProApp
          cd VisionProApp

          # Initialize a new SwiftUI project using xcodebuild
          xcodebuild -create-xcodeproj \
            -project VisionProApp.xcodeproj \
            -target VisionProApp \
            -template SwiftUIApp \
            -platform visionOS \
            -sdk xros2.2

      - name: Add Source Files
        run: |
          cd VisionProApp

          # Create Swift source file
          mkdir -p Sources
          cat <<EOF > Sources/ContentView.swift
          import SwiftUI

          struct ContentView: View {
              var body: some View {
                  Text("Hello, Vision Pro!")
              }
          }
          EOF

          # Create main app file
          cat <<EOF > Sources/VisionProApp.swift
          import SwiftUI

          @main
          struct VisionProApp: App {
              var body: some Scene {
                  WindowGroup {
                      ContentView()
                  }
              }
          }
          EOF

      - name: Verify VisionOS 2.2 SDK
        run: |
          echo "Checking available SDKs..."
          if ! xcodebuild -showsdks | grep -q "xros2.2"; then
            echo "VisionOS 2.2 SDK (xros2.2) not found!"
            exit 1
          else
            echo "VisionOS 2.2 SDK (xros2.2) is available."
          fi

      - name: Build Vision Pro App
        run: |
          cd VisionProApp
          echo "Building Vision Pro App..."
          xcodebuild clean build \
            -project VisionProApp.xcodeproj \
            -scheme VisionProApp \
            -sdk xros2.2 \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Archive Vision Pro App
        run: |
          cd VisionProApp
          echo "Archiving Vision Pro App..."
          xcodebuild archive \
            -project VisionProApp.xcodeproj \
            -scheme VisionProApp \
            -sdk xros2.2 \
            -configuration Release \
            -archivePath build/VisionProApp.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Create ExportOptions.plist
        run: |
          echo "Creating ExportOptions.plist..."
          cat <<EOF > VisionProApp/ExportOptions.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>development</string>
              <key>signingStyle</key>
              <string>automatic</string>
              <key>stripSwiftSymbols</key>
              <true/>
          </dict>
          </plist>
          EOF

      - name: Export Vision Pro App
        run: |
          cd VisionProApp
          echo "Exporting Vision Pro App..."
          xcodebuild -exportArchive \
            -archivePath build/VisionProApp.xcarchive \
            -exportPath build/output \
            -exportOptionsPlist ExportOptions.plist

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: VisionProApp
          path: VisionProApp/build/output
